 LogRequestToS3←{
     request←⍵
     ob←aws.S3.Object.New BucketRef
     ⍝ob.ACL←'public-read'
     ob.ContentType←'text/plain'
     ob.Value←request.Header
     ob.Folder←'logs'
     ob.Key←'request-',(' '~⍨∊⍕⎕TS),'.txt'
     ok←ob.Put
     ⍺.metadata.requestLogUri←ob.URI
 }

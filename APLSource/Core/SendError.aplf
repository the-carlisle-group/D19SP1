 SendError←{
     response←⍺
     dmx←⍵
     dmx.APLVersion←#.APLVersion
     dmx.Log←⎕NS''
     dmx.Log.Request←⎕NS''
     dmx.Log.Request.URI←⍺.logURI
     response.StatusCode←500
     response.Content←⎕JSON dmx
     response.ContentType←'application/json'
     _←⎕WA
     response
 }
